import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.educativo.preinscritos',
  appName: 'PREINSCRITOS',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
